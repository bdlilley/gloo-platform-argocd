---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gw-self-signed
  namespace: gloo-system
spec:
  secretName: gw-self-signed
  duration: 2160h0m0s # 90d
  renewBefore: 360h0m0s # 15d
  commonName: gw-self-signed
  dnsNames:
  - '*'
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
    group: cert-manager.io
---
apiVersion: gateway.solo.io/v1
kind: VirtualService
metadata:
  name: sample
  namespace: gloo-system
spec:
  sslConfig:
    secretRef:
      name: upstream-tls
      namespace: gloo-system
  virtualHost:
    domains:
    - '*'
    routes:
    - matchers:
      - exact: /hello
      directResponseAction:
        status: 200
        body: hello, world
    - matchers:
      - prefix: /httpbin
      options:
        prefixRewrite: '/'
      routeAction:
        single:
          upstream:
            name: httpbin-httpbin-8000
            namespace: gloo-system
